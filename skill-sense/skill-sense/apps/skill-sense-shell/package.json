{
  "name": "skill-sense-shell",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "lint": "echo 'Lint not configured yet'",
    "deploy": "npm run build && firebase deploy --only hosting"
  }
}
